{
  "type": "array",
  "title": "Commercetools lineItemReturnItemDrafts",
  "items": {
    "type": "object",
    "required": [
      "quantity",
      "lineItemId",
      "shipmentState"
    ],
    "properties": {
      "quantity": {
        "type": "integer"
      },
      "comment": {
        "type": "string"
      },
      "lineItemId": {
        "type": "string"
      },
      "shipmentState": {
        "type": "string",
        "enum": ["Advised", "Returned", "BackInStock", "Unusable"]
      },
      "returnDate": {
        "type": "string",
        "format": "date-time"
      }
    }
  }
}
